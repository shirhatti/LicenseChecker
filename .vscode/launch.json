{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Launch (console)",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceRoot}/src/shirhatti.LicenseChecker/bin/Debug/netcoreapp1.0/dotnet-check-license.dll",
            "args": [ "${workspaceRoot}\\LICENSE.template" ],
            "cwd": "${workspaceRoot}",
            "stopAtEntry": false
        }
    ]
}